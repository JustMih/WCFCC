const { Sequelize } = require("sequelize"); require("dotenv").config(); console.log("Testing connection to:", process.env.DB_HOST); const sequelize = new Sequelize(process.env.DB_NAME, process.env.DB_USER, process.env.DB_PASS, { host: process.env.DB_HOST, port: 3306, dialect: "mysql", logging: false, dialectOptions: { connectTimeout: 60000 } }); sequelize.authenticate().then(() => { console.log("✅ Connected!"); sequelize.close(); }).catch(err => { console.error("❌ Failed:", err.message); });
